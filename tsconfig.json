{
    "compilerOptions": {
        //  重定向输出目录。
        // "outDir": "./dist/",
        //  指定生成哪个模块系统代码,可选值："None"，"CommonJS"，"AMD"，"System"，"UMD"，"ES6"或"ES2015"。
        "module": "es6",
        //  指定ECMAScript目标版本（默认"ES3"）,可选值:"ES5"，"ES6"/"ES2015"，"ES2016"，"ES2017"或"ESNext"。
        "target": "es5",
        //  决定如何处理模块。或者是"Node"对于Node.js/io.js，或者是"Classic"（默认）。
        "moduleResolution": "Node",
        //  在表达式和声明上有隐含的 any类型时报错。(由strict代替)
        // "noImplicitAny": true,
        //  启用所有严格检查选项。
        // "strict": true,
        //  在.tsx文件里支持JSX："react"或"preserve"或"react-native"。查看JSX。
        "jsx": "preserve",
        // 允许编译javascript文件。
        "allowJs": false,
        //  删除所有注释，除了以/!*开头的版权信息。
        "removeComments": true,
        //  生成相应的.d.ts文件。
        "declaration": false,
        //  生成相应的.map文件。
        "sourceMap": true,
        //  编译过程中需要引入的库文件的列表。
        "lib": [
            "dom",
            "es5",
            "es6"
        ]
    },
}